export function listenForJsonRpcResponseMessage(handler, requestId) {
    if (typeof window === 'undefined')
        throw new Error('`listenForJsonRpcResponseMessage` must be called in the Client Component.');
    const listener = (event) => {
        if (event.data.id !== requestId ||
            !('result' in event.data || 'error' in event.data))
            return;
        handler(event.data);
    };
    window.addEventListener('message', listener);
    return () => window.removeEventListener('message', listener);
}
//# sourceMappingURL=listenForJsonRpcResponseMessage.js.map