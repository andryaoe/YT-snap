import { postJsonRpcRequestMessage, } from './jsonRpc/postJsonRpcRequestMessage.js';
export function postSendTransactionRequestMessage(parameters, requestIdOverride) {
    const { chainId, attribution, abi, data, gas, to, value } = parameters;
    const sendTransactionParams = {
        abi,
        data,
        to,
    };
    if (gas)
        sendTransactionParams.gas = gas.toString();
    if (value)
        sendTransactionParams.value = value.toString();
    return postJsonRpcRequestMessage('fc_requestWalletAction', {
        action: {
            method: 'eth_sendTransaction',
            attribution,
            chainId,
            params: sendTransactionParams,
        },
    }, requestIdOverride);
}
//# sourceMappingURL=postSendTransactionRequestMessage.js.map