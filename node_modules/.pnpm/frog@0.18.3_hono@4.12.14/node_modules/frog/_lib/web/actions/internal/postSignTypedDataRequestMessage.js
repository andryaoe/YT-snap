import { postJsonRpcRequestMessage, } from './jsonRpc/postJsonRpcRequestMessage.js';
export function postSignTypedDataRequestMessage(parameters, requestIdOverride) {
    const { chainId, domain, message, types, primaryType } = parameters;
    return postJsonRpcRequestMessage('fc_requestWalletAction', {
        action: {
            method: 'eth_signTypedData_v4',
            chainId,
            params: {
                domain,
                message: JSON.parse(JSON.stringify(message, (_, v) => typeof v === 'bigint' ? v.toString() : v)),
                types,
                primaryType,
            },
        },
    }, requestIdOverride);
}
//# sourceMappingURL=postSignTypedDataRequestMessage.js.map